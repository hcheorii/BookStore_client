import styled from "styled-components";

function BooksEmpty() {
    return (
        <div>
            <h1>BooksEmpty</h1>
        </div>
    );
}

const BooksStyle = styled.div``;

export default BooksEmpty;
